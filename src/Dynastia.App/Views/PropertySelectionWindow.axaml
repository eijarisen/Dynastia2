<Window
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="using:Dynastia.App.Controls"
    xmlns:vm="using:Dynastia.App.ViewModels"
    x:Class="Dynastia.App.Views.PropertySelectionWindow"
    Width="1000"
    Height="760"
    MinWidth="840"
    MinHeight="650"
    CanResize="False"
    ShowInTaskbar="False"
    WindowDecorations="None"
    TransparencyLevelHint="Transparent"
    WindowStartupLocation="CenterOwner"
    Background="#00000000"
    Title="Select Property">

    <Window.Styles>
        <Style Selector="Button.paperAction">
            <Setter Property="MinWidth" Value="120" />
            <Setter Property="Padding" Value="18,9" />
            <Setter Property="Foreground" Value="#3F2714" />
            <Setter Property="Background" Value="#E8D5AF" />
            <Setter Property="BorderBrush" Value="#9D6F35" />
            <Setter Property="BorderThickness" Value="1.5" />
            <Setter Property="FontFamily" Value="Georgia" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontWeight" Value="Bold" />
        </Style>

        <Style Selector="Button.paperAction:pointerover">
            <Setter Property="Background" Value="#D4B178" />
            <Setter Property="Foreground" Value="#2D1A0E" />
        </Style>

        <Style Selector="TextBox.paperSearch">
            <Setter Property="Background" Value="#B8F4E4C5" />
            <Setter Property="Foreground" Value="#352110" />
            <Setter Property="BorderBrush" Value="#8F6A34" />
            <Setter Property="BorderThickness" Value="1.5" />
            <Setter Property="FontFamily" Value="Georgia" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Padding" Value="10,8" />
        </Style>

        <Style Selector="ListBox.compact Border.selectionOption">
            <Setter Property="Margin" Value="2" />
            <Setter Property="Padding" Value="10,5" />
        </Style>

        <Style Selector="ListBox.compact TextBlock.optionPrimary">
            <Setter Property="FontSize" Value="14" />
        </Style>

        <Style Selector="ListBox.compact TextBlock.optionSecondary">
            <Setter Property="FontSize" Value="11.5" />
        </Style>
    </Window.Styles>

    <Grid>
        <Image
            Source="/Assets/UI/instructions_paper.png"
            Stretch="Fill" />

        <Grid
            Margin="80,72,80,58"
            RowDefinitions="Auto,Auto,*,Auto"
            RowSpacing="14">

            <StackPanel
                HorizontalAlignment="Center"
                Spacing="2">
                <TextBlock
                    x:Name="TitleText"
                    HorizontalAlignment="Center"
                    FontFamily="avares://Dynastia.App/Assets/Fonts#UnifrakturCook"
                    FontSize="42"
                    Foreground="#2F1D0E"
                    Text="Select Property" />
                <TextBlock
                    x:Name="ContextText"
                    IsVisible="False"
                    HorizontalAlignment="Center"
                    FontFamily="Georgia"
                    FontSize="13"
                    FontWeight="SemiBold"
                    Foreground="#5A4024"
                    TextAlignment="Center" />
            </StackPanel>

            <TextBox
                x:Name="SearchBox"
                Grid.Row="1"
                Classes="paperSearch"
                PlaceholderText="Search town, county, region or opportunity..."
                TextChanged="OnSearchTextChanged" />

            <Border
                Grid.Row="2"
                Padding="8"
                Background="#0FFFFFFF"
                BorderBrush="#8F6A34"
                BorderThickness="1.5"
                CornerRadius="6">

                <ListBox
                    x:Name="OptionsList"
                    Background="Transparent"
                    BorderThickness="0"
                    SelectionChanged="OnSelectionChanged">
                    <ListBox.ItemTemplate>
                        <DataTemplate x:DataType="vm:PropertySelectionOption">
                            <Border
                                Classes="selectionOption"
                                Margin="3"
                                Padding="12,9"
                                Opacity="{Binding DisplayOpacity}"
                                Background="#28FFF8E8"
                                BorderBrush="#A78959"
                                BorderThickness="0,0,0,1">
                                <Grid ColumnDefinitions="*,Auto" ColumnSpacing="18">
                                    <StackPanel Spacing="3">
                                        <TextBlock
                                            Classes="optionPrimary"
                                            FontFamily="Georgia"
                                            FontSize="16"
                                            FontWeight="Bold"
                                            Foreground="#352615"
                                            Text="{Binding PrimaryText}" />
                                        <TextBlock
                                            Classes="optionSecondary"
                                            FontFamily="Georgia"
                                            FontSize="12"
                                            Foreground="#6B5434"
                                            Text="{Binding SecondaryText}" />
                                        <TextBlock
                                            IsVisible="{Binding HasDetailsText}"
                                            Margin="0,4,0,0"
                                            FontFamily="Georgia"
                                            FontSize="12"
                                            LineHeight="17"
                                            Foreground="#46351F"
                                            TextWrapping="Wrap"
                                            Text="{Binding DetailsText}" />
                                    </StackPanel>
                                    <TextBlock
                                        Grid.Column="1"
                                        VerticalAlignment="Center"
                                        FontFamily="Georgia"
                                        FontSize="15"
                                        FontWeight="Bold"
                                        Foreground="#694914"
                                        Text="{Binding PriceText}" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>

            <StackPanel
                x:Name="ImageActionPanel"
                Grid.Row="3"
                HorizontalAlignment="Center"
                Orientation="Horizontal"
                Spacing="12"
                IsVisible="False">
                <ui:ImageStateButton
                    x:Name="ImageConfirmButton"
                    Width="160"
                    Height="66"
                    FallbackText="Confirm"
                    Click="OnConfirmClick" />
                <ui:ImageStateButton
                    Width="160"
                    Height="66"
                    IdleSource="avares://Dynastia.App/Assets/UI/b_close_idle.png"
                    HoverSource="avares://Dynastia.App/Assets/UI/b_close_hover.png"
                    FallbackText="Close"
                    Click="OnCancelClick" />
            </StackPanel>

            <StackPanel
                x:Name="GenericActionPanel"
                Grid.Row="3"
                HorizontalAlignment="Center"
                Orientation="Horizontal"
                Spacing="12">
                <Button
                    Classes="paperAction"
                    Content="Cancel"
                    Click="OnCancelClick" />
                <Button
                    x:Name="ConfirmButton"
                    Classes="paperAction"
                    Content="Confirm"
                    Click="OnConfirmClick" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
